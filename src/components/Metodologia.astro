---
/*
  📘 Venados Atalaya — Sección Metodología
  Autor: MhegasDev
  Descripción: Filosofía de entrenamiento con enfoque técnico, mental y en valores.
  Versión: optimizada visualmente para tema claro/oscuro con profundidad profesional.
*/
  import { withBase } from "@/utils/withBase";
---

<section
  id="metodologia"
  class="relative py-24 section-bg-a text-[var(--color-light)] overflow-hidden"
  aria-labelledby="titulo-metodologia"
>
  <!-- 🌫️ Degradado superior -->
  <div class="absolute top-0 inset-x-0 h-24 bg-gradient-to-b from-[var(--color-primary)]/10 to-transparent pointer-events-none"></div>

  <!-- 🏷️ TÍTULO -->
  <header class="text-center mb-16 relative z-10">
    <h2
      id="titulo-metodologia"
      class="text-4xl md:text-5xl font-extrabold tracking-wide mb-4 leading-tight text-[var(--color-primary)] dark:text-[var(--color-accent)]"
    >
      NUESTRA <span class="text-[var(--color-secondary)]">METODOLOGÍA</span>
    </h2>
    <div class="flex justify-center mt-4 mb-8">
      <div class="w-32 h-[3px] bg-[var(--color-accent)] rounded-full shadow-[0_0_15px_rgba(255,215,0,0.6)]"></div>
    </div>
    <p
      class="max-w-3xl mx-auto text-lg md:text-xl font-light leading-relaxed text-[var(--color-light)]/80 dark:text-white/80"
    >
      Un sistema integral que forma futbolistas completos: técnica, mente y valores.<br />
      Cada entrenamiento potencia cuerpo, mente y espíritu de equipo.
    </p>
  </header>

  <!-- ⚙️ CONTENIDO PRINCIPAL -->
  <div
    class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center px-6 relative z-10"
  >
    <!-- 🧠 TEXTO -->
    <div class="space-y-10">
      {[
        {
          id: 1,
          titulo: "Formación Integral",
          texto:
            "Fomentamos la técnica, el respeto y la pasión. Cada sesión desarrolla la mente, el cuerpo y el compañerismo, creando una base sólida en valores y disciplina.",
        },
        {
          id: 2,
          titulo: "Progresión por Etapas",
          texto:
            "Adaptamos la enseñanza según la edad y nivel del jugador. Desde la coordinación básica hasta la táctica avanzada, garantizando una evolución natural y constante.",
        },
        {
          id: 3,
          titulo: "Enfoque Científico",
          texto:
            "Aplicamos principios de entrenamiento deportivo, nutrición y psicología infantil. Cada sesión está planificada para maximizar el rendimiento y prevenir lesiones.",
        },
      ].map((bloque) => (
        <article
          key={bloque.id}
          class="flex items-start gap-4 group bg-white/5 dark:bg-black/10 p-4 rounded-xl transition-all duration-500 hover:bg-[var(--color-primary)]/15 hover:-translate-y-1"
        >
          <div
            class="bg-[var(--color-accent)]/10 text-[var(--color-accent)] w-12 h-12 rounded-full flex items-center justify-center text-xl font-bold shadow-inner transition-transform duration-500 group-hover:scale-110"
          >
            {bloque.id}
          </div>
          <div>
            <h3
              class="text-xl font-semibold mb-2 text-[var(--color-accent)] tracking-wide"
            >
              {bloque.titulo}
            </h3>
            <p class="text-[var(--color-light)]/85 dark:text-white/85 text-sm md:text-base leading-relaxed">
              {bloque.texto}
            </p>
          </div>
        </article>
      ))}
    </div>

    <!-- 📸 IMAGEN PRINCIPAL -->
    <figure class="relative group">
      <img
        src="img/metodologia.webp"
        alt="Entrenamiento de jóvenes futbolistas en campo de práctica"
        class="rounded-2xl shadow-[0_15px_40px_rgba(0,0,0,0.25)] w-full h-[420px] object-cover brightness-[0.9] group-hover:brightness-105 transition-all duration-700"
        loading="lazy"
        decoding="async"
      />
      <figcaption
        class="absolute bottom-4 right-6 text-xs text-light/70 italic tracking-wide"
      >
        Formación táctica y técnica — Venados Atalaya
      </figcaption>
      <div
        class="absolute inset-0 bg-gradient-to-t from-[var(--color-primary)]/30 via-transparent to-transparent rounded-2xl pointer-events-none"
      ></div>
    </figure>
  </div>

  <!-- ✨ ANIMACIÓN DE ENTRADA -->
  <script is:inline>
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("opacity-100", "translate-y-0");
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.2 }
    );

    document
      .querySelectorAll("#metodologia article, #metodologia figure")
      .forEach((el) => {
        el.classList.add(
          "opacity-0",
          "translate-y-10",
          "transition-all",
          "duration-700",
          "ease-out"
        );
        observer.observe(el);
      });
  </script>
</section>
